#include <stdio.h>
#include <stdlib.h>
#include "tiles.h"
/*
int IsNonTraversible(int tile)
{
    for(int i = 0; i < len(nontraversible); i++)
    {
        if(tile == nontraversible[i])
        {
            return 1;
        }
    }

    return 0;
}

int BlocksView(int tile)
{
    for(int i = 0; i < len(blocksview); i++)
    {
        if(tile == blocksview[i])
        {
            return 1;
        }
    }

    return 0;
}

int IsDifficultTerrain(int tile)
{
    for(int i = 0; i < len(difficultterrain); i++)
    {
        if(tile == difficultterrain[i])
        {
            return 1;
        }
    }

    return 0;
}*/